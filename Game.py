def Game():
    while True:
        Move()
        #Client
        if Play()=True:
            #Checks wether move is valid
            break
    while True:
        Check()
        if Rules!=-1:
            if Rules='+':
                #can send back to Move()
            elif Rules='0':

            elif Rules='*':

            elif Rules=1:
                #Can't Play draws a card
